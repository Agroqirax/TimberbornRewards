namespace Agroqirax.Rewards
{
    /// <summary>Which character type a <see cref="NeedReward"/> targets.</summary>
    public enum NeedCharacterTarget
    {
        /// <summary>Apply to beavers only.</summary>
        Beaver,
        /// <summary>Apply to bots only.</summary>
        Bot,
    }
}